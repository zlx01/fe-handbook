# 编排与 Kubernetes

编排平台负责调度容器、服务发现、配置、扩缩容、故障恢复和滚动部署。Kubernetes 是本章的主要实现，Helm/Kustomize 和 GitOps 用于管理声明式配置。

## 推荐学习顺序

`Pod → Deployment → Service → ConfigMap/Secret → 探针与资源 → Ingress/Gateway → 存储 → RBAC → Helm/GitOps → 可观测与排障`
